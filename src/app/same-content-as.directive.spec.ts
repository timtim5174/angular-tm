import { SameContentAsDirective } from './same-content-as.directive';

describe('SameContentAsDirective', () => {
  it('should create an instance', () => {
    const directive = new SameContentAsDirective();
    expect(directive).toBeTruthy();
  });
});
